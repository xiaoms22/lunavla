# v1.0 media archive

These assets supported the initial homepage and portfolio story. They are not generated by the v1.1 NumPy teaching core and are not current experiment evidence.

- `pusht_act_eval.gif` and `pusht_diffusion_policy_eval.gif` are historical external visual references, not LunaVLA rollouts.
- `homepage_results.svg` and `policy_ladder.svg` contain pre-correction labels or metrics.
- `lunavla-architecture.svg` uses pre-correction ACT-style terminology.
- asset manifests point to transient v1.0 output paths.

Current evidence lives under `results/v1.1/` and is rendered from validated manifests.
